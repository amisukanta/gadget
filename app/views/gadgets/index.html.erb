<h1>My gadgets</h1>
<div class="row">
	



<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>

      <th>Price</th>
      <th>Image</th>

      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gadgets.each do |gadget| %>
      <tr>
        <td><%= gadget.name %></td>
        <td><%= gadget.description %></td>

        <td><%= gadget.price %></td>
        <td><%#= gadget.avatar_url(:thumb) %></td>
        
        

        <td><%= link_to 'Show', gadget %></td>
        <td><%= link_to 'Edit', edit_gadget_path(gadget) %></td>
        <td><%= link_to 'Destroy', gadget, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>


<%= link_to 'New Gadget', new_gadget_path %>
